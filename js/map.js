class Map {
    constructor(divMap, zoom, center) {
        this.divMap = divMap;
        this.zoom = zoom;
        this.center = center;
        this.stations = [];
    }

    initMap() {};
    getApiStation(){
        
    };
}
